# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_PORT=5432

# JWT Configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION=24h

# CORS Configuration
CORS_ORIGIN=https://your-frontend-url.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Client URL
CLIENT_URL=http://localhost:5173

# GitHub Configuration (if needed)
GITHUB_TOKEN=your_github_token

# Note: In production, make sure to:
# 1. Use strong, unique values for JWT_SECRET
# 2. Set appropriate CORS_ORIGIN and CLIENT_URL values
# 3. Configure secure database credentials
# 4. Adjust rate limiting as needed for your use case
